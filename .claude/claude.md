# Claude Code 项目指南

## MDX 文件编辑规范

### 🚨 常见错误预防

#### 1. Unexpected character `<` 错误

**错误示例：**
```mdx
Error evaluating Node.js code
465:3: Unexpected character `<` (U+003C) after name, expected a character that can start an attribute name
```

**常见原因：**

1. **JSX 组件标签重复或未闭合**
   ```mdx
   ❌ 错误：
   <Card
   <Card
     title="something"
   />

   ✅ 正确：
   <Card
     title="something"
   />
   ```

2. **使用 sed 批量插入时位置错误**
   - 使用 `sed` 在固定行号插入 JSX 组件容易导致标签重复
   - **解决方案**：使用 Edit 工具精确替换，或先 Read 文件确认结构

3. **比较运算符未转义**
   ```mdx
   ❌ 错误：
   if (x > y && z < 10)

   ✅ 正确：
   if (x &gt; y && z &lt; 10)
   ```

### ✅ 最佳实践

#### 批量编辑 MDX 文件时的规则

1. **优先使用 Edit 工具而非 sed**
   ```typescript
   // 好的做法
   Edit(file_path, old_string, new_string)

   // 避免的做法
   sed -i '行号 a\<Card...' file.mdx  // 容易导致重复标签
   ```

2. **修改前必须 Read**
   - 在编辑前使用 Read 工具读取文件
   - 确认目标位置的确切内容
   - 使用精确的字符串匹配而非行号

3. **插入 JSX 组件的安全流程**
   ```
   步骤 1: Read 文件，找到插入位置
   步骤 2: 复制完整的上下文字符串
   步骤 3: 使用 Edit 工具进行精确替换
   步骤 4: 验证修改（再次 Read 或运行 build）
   ```

4. **多语言同步更新策略**
   - 先更新英文版（en/）
   - 验证构建成功
   - 再逐一更新其他语言版本
   - 每个语言版本都单独验证

### 🔍 MDX 语法检查清单

编辑 MDX 文件后，检查以下内容：

- [ ] JSX 组件标签成对出现（开始和结束标签）
- [ ] 没有重复的开始标签
- [ ] 比较运算符 `>` `<` 已转义为 `&gt;` `&lt;`
- [ ] 代码块使用正确的围栏语法 ` ```language `
- [ ] import 语句在文件顶部
- [ ] frontmatter 格式正确（YAML 语法）

### 🛠️ 调试技巧

**遇到 MDX 错误时：**

1. **找到错误行号**
   - 错误信息会显示具体行号，如 `465:3`
   - 使用 `Read` 工具查看该行及上下文

2. **常见修复方法**
   ```bash
   # 检查标签配对
   grep -n "<Card" file.mdx | wc -l
   grep -n "</Card>" file.mdx | wc -l  # 数量应该相等

   # 查找未闭合的组件
   grep -n "<[A-Z]" file.mdx  # 找到所有组件标签
   ```

3. **使用 diff 对比**
   - 对比成功的文件（如英文版）和失败的文件
   - 找出结构差异

## 项目特定注意事项

### Fumadocs MDX 组件

常用组件及正确用法：

```mdx
// Callout 组件
<Callout type="info">
  内容
</Callout>

// Cards 组件
<Cards>
  <Card title="标题" description="描述" icon="图标" />
  <Card title="标题" href="/链接" />
</Cards>

// Steps 组件
<Steps>
  <Step>
    步骤内容
  </Step>
</Steps>

// Files 组件
<Files>
  <Folder name="文件夹" defaultOpen>
    <File name="文件名" description="描述" />
  </Folder>
</Files>
```

### Frontmatter 规范

```yaml
---
title: "标题"
description: "描述"
image: /images/docs/slug/image.svg
lang: zh  # 可选
category: development  # 可选
difficulty: intermediate  # 可选
tags:
  - tag1
  - tag2
---
```

**注意：**
- `image` 字段必须在 tags 列表之后
- 不要在 tags 列表中间插入其他字段
- YAML 列表使用 `  - ` 格式（两个空格+连字符+空格）

## 自动化工作流规范

### 使用 skill-article-publisher

发布前自动验证：
- MDX 语法检查
- 比较运算符转义
- 构建验证（npm run build）
- Frontmatter 结构验证

### 使用 philosophical-illustrator

生成 SVG 插图：
- 自动分析文章主题
- 选择合适的调色板
- 生成并保存 SVG
- 更新 frontmatter 的 image 字段

---

**最后更新**: 2025-11-18
**维护者**: Claude Code Team
