# MCP 模式下运行 Fumadocs Article Importer - 测试指南

完整的 MCP 配置和测试流程，确保 Skill 在 MCP 模式下能够正常运行。

---

## 📋 测试目标

验证以下功能：
1. ✅ Translator MCP 连接成功
2. ✅ Jina MCP 连接成功（可选）
3. ✅ Skill 能够检测到 MCP 工具
4. ✅ translate_text 工具可用并正常工作
5. ✅ read_url 工具可用（如果配置了 Jina MCP）
6. ✅ 完整的文章导入流程能够跑通

---

## 🚀 快速开始（5 分钟设置）

### Step 1: 配置 Claude Desktop

**编辑配置文件**：

```bash
# macOS
vim ~/Library/Application\ Support/Claude/claude_desktop_config.json

# Linux
vim ~/.config/Claude/claude_desktop_config.json

# Windows (PowerShell)
notepad $env:APPDATA\Claude\claude_desktop_config.json
```

**添加以下配置**：

```json
{
  "mcpServers": {
    "translator": {
      "url": "https://t.deeptoai.com/sse",
      "transport": "sse"
    },
    "jina": {
      "url": "https://mcp.jina.ai/sse",
      "transport": "sse"
    }
  }
}
```

**保存并退出**

### Step 2: 重启 Claude

- **Claude Desktop**: 完全退出（Cmd+Q / Alt+F4）并重新打开
- **Claude Code CLI**: 退出当前会话，重新启动

### Step 3: 验证 MCP 连接

在 Claude 中输入：

```
列出所有可用的 MCP 工具
```

**期望看到**：

```
可用的 MCP 工具：

Translator MCP:
✅ translate_text
✅ evaluate_translation

Jina MCP:
✅ read_url
✅ guess_datetime_url
✅ search_web
... (更多工具)
```

**如果看到这些工具 → 配置成功！继续下一步**

**如果没看到 → 跳到"故障排查"部分**

---

## 🧪 功能测试

### 测试 1: Translator MCP - 基础翻译

**输入**：
```
使用 translate_text 工具翻译以下内容到中文：

"Hello, this is a test of the translator MCP server."
```

**期望结果**：
```
✅ 工具被调用
✅ 返回中文翻译："你好，这是翻译 MCP 服务器的测试。"
✅ 耗时 3-10 秒
```

**验证点**：
- [ ] 工具成功调用
- [ ] 翻译准确
- [ ] 速度合理

---

### 测试 2: Translator MCP - 技术文档翻译

**输入**：
```
使用 translate_text 翻译到中文（high_quality: true）：

"React Hooks allow you to use state and other React features without writing a class. The useState hook lets you add state to function components."
```

**期望结果**：
```
✅ 三阶段翻译流程：
   - Stage 1: 分析和规划
   - Stage 2: 分段翻译
   - Stage 3: 全文审查
✅ 专业术语处理正确（React Hooks, useState 保持或适当翻译）
✅ 翻译质量高
```

**验证点**：
- [ ] 技术术语处理得当
- [ ] 中文表达自然流畅
- [ ] 保持技术准确性

---

### 测试 3: Translator MCP - 保留 Markdown

**输入**：
```
使用 translate_text 翻译到法语：

**Important**: This is a `code example` with **bold** text.

```javascript
console.log('Hello World');
```
```

**期望结果**：
```
✅ Markdown 语法完整保留
✅ 代码块未被翻译
✅ 格式完全一致
```

**验证点**：
- [ ] `**bold**` 语法保留
- [ ] `` `code` `` 语法保留
- [ ] 代码块未被修改
- [ ] "Important" 被翻译成 "Important" 或 "Remarque"

---

### 测试 4: Translator MCP - 质量评估

**输入**：
```
使用 evaluate_translation 评估翻译质量：

原文: "Python is a versatile programming language."
译文: "Python 是一种多功能的编程语言。"

请提供详细反馈（detailed_feedback: true）
```

**期望结果**：
```json
{
  "overall_score": 90-100,
  "accuracy": { "score": 95-100, "feedback": "..." },
  "fluency": { "score": 90-100, "feedback": "..." },
  "terminology": { "score": 95-100, "feedback": "..." },
  "style": { "score": 90-100, "feedback": "..." }
}
```

**验证点**：
- [ ] 返回四个维度的评分
- [ ] 每个维度有具体反馈
- [ ] 总分合理

---

### 测试 5: Jina MCP - 文章获取

**输入**：
```
使用 read_url 工具获取以下网页内容：

https://example.com
```

**期望结果**：
```
✅ 返回 Markdown 格式的网页内容
✅ HTML 被清理（无广告、导航等）
✅ 图片引用保留
✅ 耗时 3-5 秒
```

**验证点**：
- [ ] Markdown 格式正确
- [ ] 内容干净（无噪音）
- [ ] 速度合理

---

### 测试 6: Jina MCP - 日期检测

**输入**：
```
使用 guess_datetime_url 获取发布日期：

https://dev.to/some-article
```

**期望结果**：
```json
{
  "published": "2025-11-15T10:30:00Z",
  "updated": "2025-11-16T14:20:00Z",
  "confidence": "high"
}
```

**验证点**：
- [ ] 检测到发布日期
- [ ] 格式正确（ISO 8601）
- [ ] 置信度合理

---

## 📝 完整 Skill 测试

### 测试场景：导入一篇技术文章

**准备一个测试文章**：
- 选择一篇公开的技术博客（如 dev.to, Medium 等）
- 长度 500-1000 字
- 包含代码块和图片

**运行 Skill**：

```
我想导入这篇文章到 Fumadocs 项目：
https://dev.to/username/your-test-article

翻译到：en, zh
下载图片：yes
```

**期望流程**：

```
✅ Step 1: 获取文章信息
   → 询问 URL、语言、是否下载图片

✅ Step 2: 使用 Jina MCP read_url 下载文章
   → 返回 Markdown 格式内容
   → 耗时 3-5 秒

✅ Step 3: 使用 guess_datetime_url 获取日期
   → 返回发布日期

✅ Step 4: 生成 slug
   → 从标题生成 URL-friendly slug

✅ Step 5: 处理图片
   → 下载图片到 public/images/docs/{slug}/
   → 更新 Markdown 中的图片引用

✅ Step 6: 分类文章
   → AI 分析内容
   → 确定 category, difficulty, tags

✅ Step 7: 使用 Translator MCP translate_text 翻译
   中文翻译：
   ├─ Stage 1: 分析规划 (1-2s)
   ├─ Stage 2: 分段翻译 (3-5s)
   └─ Stage 3: 全文审查 (1-2s)
   ✅ 完成 (6-9 秒)

✅ Step 8: 生成 MDX 文件
   → content/docs/en/{category}/{slug}.mdx
   → content/docs/zh/{category}/{slug}.mdx
   → 包含完整 frontmatter

✅ Step 9: 归档
   → archive/2025-11/{slug}/
   → 保存 original.md 和 metadata.json

✅ Step 10: 生成汇总报告
```

**验证点**：
- [ ] 所有步骤成功完成
- [ ] MCP 工具被正确调用
- [ ] 文件创建正确
- [ ] 翻译质量好
- [ ] 总耗时合理（< 60 秒）

---

## 🐛 故障排查

### 问题 1: 看不到 MCP 工具

**症状**：
```
列出所有可用的 MCP 工具
→ 没有返回 translate_text 或 read_url
```

**解决步骤**：

1. **检查配置文件格式**：
```bash
# 验证 JSON 格式
python3 -m json.tool ~/Library/Application\ Support/Claude/claude_desktop_config.json
```

2. **确认配置文件位置正确**：
```bash
# macOS
ls ~/Library/Application\ Support/Claude/claude_desktop_config.json

# Linux
ls ~/.config/Claude/claude_desktop_config.json
```

3. **完全重启 Claude**：
- 不是最小化，是完全退出
- 确保进程完全关闭
- 重新打开

4. **查看 Claude 日志**（如果可用）：
```bash
# macOS
tail -f ~/Library/Logs/Claude/app.log
```

---

### 问题 2: 连接超时

**症状**：
```
Error: Connection timeout
Error: Failed to connect to MCP server
```

**解决步骤**：

1. **测试网络连接**：
```bash
# 测试 Translator MCP
curl -I https://t.deeptoai.com/sse

# 测试 Jina MCP
curl -I https://mcp.jina.ai/sse
```

2. **检查防火墙/代理**：
- 确保允许访问外部 HTTPS
- 检查公司网络是否有限制

3. **尝试使用 VPN**（如果在受限网络）

---

### 问题 3: Translator MCP 工作但速度很慢

**症状**：
- 每次翻译 > 30 秒

**解决步骤**：

1. **检查网络延迟**：
```bash
ping t.deeptoai.com
```

2. **尝试使用本地部署**：
```bash
# 使用 NPM 快速启动
npx translator-mcp-server
```

3. **然后配置 Claude 使用本地服务器**：
```json
{
  "mcpServers": {
    "translator": {
      "command": "npx",
      "args": ["translator-mcp-server"],
      "env": {
        "TRANSLATION_API_KEY": "your_key",
        "TRANSLATION_MODEL": "glm-4-flash",
        "TRANSLATION_BASE_URL": "https://open.bigmodel.cn/api/paas/v4"
      }
    }
  }
}
```

---

### 问题 4: 翻译质量不佳

**症状**：
- 技术术语错误
- Markdown 被破坏
- 翻译不流畅

**解决步骤**：

1. **确认使用 high_quality: true**：
```
translate_text(
  text: "...",
  target_language: "zh",
  high_quality: true  # 必须！
)
```

2. **使用 evaluate_translation 检查质量**：
```
evaluate_translation(
  original_text: "...",
  translated_text: "...",
  detailed_feedback: true
)
```

3. **如果质量始终不好，考虑**：
- 使用更好的模型（GLM-4.6 vs GLM-4-flash）
- 本地部署并配置更强的 LLM
- 使用 Claude 直接翻译作为 fallback

---

### 问题 5: Skill 无法检测到 MCP

**症状**：
```
⚠️  Translator MCP not found, using Claude fallback
⚠️  Jina MCP not found, using API fallback
```

**这可能是正常的！**

Skill 设计为：
1. **尝试检测 MCP**
2. **如果检测不到 → 自动降级到 fallback**
3. **仍然能正常工作**

**验证 MCP 确实可用**：
```
直接测试 MCP 工具：
"使用 translate_text 翻译 'Hello' 到中文"

如果这个能工作 → MCP 可用
如果这个不工作 → MCP 配置有问题
```

---

## ✅ 测试检查清单

完成以下测试后，你的 MCP 配置就没问题了：

### 基础连接
- [ ] Claude 能列出 MCP 工具
- [ ] translate_text 工具可见
- [ ] read_url 工具可见（如果配置了 Jina）

### Translator MCP 功能
- [ ] 基础翻译工作（测试 1）
- [ ] 技术文档翻译质量好（测试 2）
- [ ] Markdown 语法被保留（测试 3）
- [ ] 质量评估工具可用（测试 4）

### Jina MCP 功能
- [ ] 文章获取工作（测试 5）
- [ ] 日期检测工作（测试 6）

### 完整 Skill
- [ ] 完整的文章导入流程成功
- [ ] MCP 工具被正确调用
- [ ] 性能符合预期（< 60 秒总耗时）
- [ ] 生成的文件正确

---

## 📊 性能基准

**使用 MCP 的预期性能**（1 篇文章，2 种语言）：

```
Step 2: 文章获取 (Jina MCP)         3-5 秒
Step 4: 图片下载                    3-5 秒
Step 5: 分类                       1-2 秒
Step 6: 翻译 - 中文 (Translator)    6-9 秒
Step 7: 生成 MDX                   1-2 秒
Step 8: 归档                       1 秒
────────────────────────────────────────
总计                               15-24 秒 ⚡
```

**不使用 MCP 的性能**（fallback）：

```
Step 2: 文章获取 (curl)            8-12 秒
Step 4: 图片下载                   3-5 秒
Step 5: 分类                      1-2 秒
Step 6: 翻译 - Claude 直接          25-35 秒
Step 7: 生成 MDX                  1-2 秒
Step 8: 归档                      1 秒
────────────────────────────────────────
总计                              39-57 秒 🐌
```

**速度提升：约 2-3 倍**

---

## 🎯 成功标准

**MCP 模式配置成功的标志**：

✅ **必需（Must Have）**:
- Claude 能列出 MCP 工具
- translate_text 基础功能正常
- Skill 能完成完整导入流程

✅ **推荐（Should Have）**:
- 所有 6 个功能测试通过
- 性能符合基准（< 30 秒，2 语言）
- 翻译质量评分 > 85

✅ **可选（Nice to Have）**:
- read_url 正常工作
- guess_datetime_url 准确
- 支持 4 种语言翻译

---

## 📝 测试报告模板

**完成测试后填写**：

```markdown
## MCP 模式测试报告

### 环境
- 操作系统: ___________
- Claude 版本: ___________
- 配置模式: [ ] 公共服务器  [ ] 本地部署

### MCP 连接状态
- Translator MCP: [ ] ✅ 连接  [ ] ❌ 失败
- Jina MCP: [ ] ✅ 连接  [ ] ❌ 失败

### 功能测试结果
- 测试 1 (基础翻译): [ ] 通过  [ ] 失败
- 测试 2 (技术文档): [ ] 通过  [ ] 失败
- 测试 3 (Markdown): [ ] 通过  [ ] 失败
- 测试 4 (质量评估): [ ] 通过  [ ] 失败
- 测试 5 (文章获取): [ ] 通过  [ ] 失败
- 测试 6 (日期检测): [ ] 通过  [ ] 失败

### 完整 Skill 测试
- 测试文章 URL: ___________
- 翻译语言: ___________
- 总耗时: _____ 秒
- 结果: [ ] 成功  [ ] 部分成功  [ ] 失败

### 性能数据
- 文章获取: _____ 秒
- 翻译（每语言）: _____ 秒
- 总耗时: _____ 秒

### 遇到的问题
1. ___________
2. ___________

### 结论
[ ] ✅ MCP 模式完全可用
[ ] ⚠️  MCP 部分可用（需调整）
[ ] ❌ MCP 不可用（使用 fallback）

说明: ___________
```

---

## 🚀 下一步

**MCP 模式测试通过后**：

1. **提交测试报告** - 分享你的测试结果
2. **优化配置** - 根据性能调整
3. **实际使用** - 导入真实文章
4. **反馈改进** - 报告任何问题

**如果遇到问题**：

1. **查看故障排查部分**
2. **尝试 fallback 模式**（不使用 MCP）
3. **报告问题**到 GitHub Issues

---

**准备好开始测试了吗？** 🎯

从 **Step 1: 配置 Claude Desktop** 开始！
